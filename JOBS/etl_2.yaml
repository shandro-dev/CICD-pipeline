resources:
  jobs:
    CICD_test:
      name: CICD test_2
      tasks:
        - task_key: task1
          notebook_task:
            notebook_path: /Workspace/LIVE_dev/SRC/ingest_raw
            source: WORKSPACE
          environment_key: Default

        - task_key: task2
          depends_on:
            - task_key: task1
          notebook_task:
            notebook_path: /Workspace/SRC/gold.ipynb
            source: WORKSPACE
      queue:
        enabled: true
      environments:
        - environment_key: Default
          spec:
            environment_version: "5"
      performance_target: PERFORMANCE_OPTIMIZED